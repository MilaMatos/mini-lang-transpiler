// Erros Sintaticos

def calcular_media(a: real, b: real): real {
    var soma: real = a + b;
    return soma / 2.0;
}

var x: int = 10;

// Erro: Faltando parentese de fechamento no while
while (x > 0 {
    print x;
    set x = x - 1;
}