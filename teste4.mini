int x;
bool y;

x = true;      // erro: atribuição de bool para int

z = 10;        // erro: variável não declarada

int soma(int a, int b) {
    return a + b;
}

soma(1);       // erro: número de argumentos incorreto

soma(true, 2); // erro: tipo de argumento incompatível